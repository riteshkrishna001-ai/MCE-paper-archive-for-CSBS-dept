import { ComingSoon } from '@/components/layout/ComingSoon';

export default function AdminDashboard() {
  return (
    <ComingSoon
      title="Admin Dashboard"
      note="You're verified as an administrator. Pending uploads, approvals, and analytics arrive in the Data Services and Admin phases."
    />
  );
}
